#include <stdio.h>

/**
*main -  prints all single digit numbers
* of base 10 starting from 0, followed
* by a new line
* Return: always 0 (success)
*/

int main(void)
{
int i;
for (i = 18; i < 28; i++)
{
putchar(i);
}
putchar('\n');
return (0);
}

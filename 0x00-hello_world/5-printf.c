#include <stdio.h>
/**
*main: main function outputs with proper grammar, but the outcome is a piece of art
*Return- 0 if program run as expected otherwise
*/
int main()
{
printf("with proper grammar, but the outcome is a piece of art,");
return (0);
}
